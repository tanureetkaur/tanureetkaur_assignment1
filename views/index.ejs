<% include ./partials/header.ejs %>

  <div class="container">
    <div class="jumbotron">
        <h1 class="display-4">Contacts</h1>
        <% if(title == "Contact") { %>
          <a class="btn btn-primary" href="/contact-list" role="button">Link</a>

        <% }%>
            
        <h1 class="display-4">Favourite Things</h1>
        <% if(title == "Favourites") { %>
          <a class="btn btn-primary" href="/fav-Things" role="button">Go To Things</a>

        <% }%>
        

      </div>
  </div>

  <% include ./partials/footer.ejs %>